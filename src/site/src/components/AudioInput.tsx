/**
 * AudioInput.tsx
 * Component for audio input: upload or record
 * Sends audio file to backend via WebSocket Base64 encoded
 * Optional : show a waveform preview of the uploaded audio
 */
import React from 'react';

const AudioInput: React.FC = () => {
  // TODO: Implement file upload, recording, waveform preview
  return (
    <div>
      <h2>Audio Input</h2>
      {/* Upload button, progress bar, record button, recording indicator, timer, waveform preview */}
    </div>
  );
};

export default AudioInput;
